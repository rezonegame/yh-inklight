# 2026-08-22 工作日志

## 任务：sidecar 标注存储格式改为与导出标注格式一致

用户要求：数据存储文件（Markdown sidecar）中高亮/笔记的格式改为与导出标注格式一致；YAML frontmatter 不动。

### 改动（src/storage/annotationStore.ts）
- `serializeDocumentToMarkdown` 中每个标注由原先的 `# 标题 ^blockId` 一级标题改为 Obsidian callout 块，与 `renderAnnotationBlock` 导出示例如下对齐：
  - `> [!book-note-md|color] source - createdAt ^bn-{mode}-{id}`
  - 引用原文、标签、Note、replies、resolved、返回原文链接的排版与导出一致。
  - 用于无损往返的隐藏 JSON span 从 callout 外部移入 callout 内部（加 `> ` 前缀），与导出隐藏锚点视觉位置一致。
- 新增 sidecar 专用辅助函数：`sidecarSource`、`sidecarCreatedAt`、`sidecarContent`、`sidecarTagLabel`、`sidecarResolved`、`sidecarReplies`，分别对应导出时的 `entrySource` 等逻辑。
- 二次优化：按「高亮 / 笔记」分区，再按章节/页码分二级标题：
  - `# 高亮` / `# 笔记` 一级分区；
  - EPUB 用 `## 章节名`，PDF 用 `## p.页码`，Markdown 无二级标题；
  - 同一二级标题下按原顺序排列 callout。
- 同步调整导出 `renderReadingNotes`：在保留顶层 `# Notes for ...` 标题的前提下，改用 `## 高亮` / `## 笔记` 分区 + `### 章节/页码` 子标题，与 sidecar 结构保持一致。
- 新增辅助函数 `sidecarGroupHeading`、`entryGroupHeading`、`sidecarSectionTitle`。

### 改动（tests/markdownSidecar.test.ts）
- 将“每个标注是一级标题”断言改为“每个标注是 callout 块”断言，正则匹配 `^> [!book-note-(md|pdf|epub)\|...] ... ^bn-`。
- 新增测试断言 `# 高亮`、`# 笔记` 分区及 `## Ch 1 <x>`、`## Ch 2`、`## p.3`、`## p.4` 二级标题存在。

### 验证
- `npm run build` 通过。
- `node scripts/test.mjs`：19/19 pass。
- `main.js` 安全扫描：无 `createContextualFragment` / `new Function` / `eval`。

## 任务：合规检查与修复（i18n 红线）

阶段四验证发现 storage 格式中存在硬编码中文字符串，违反 obsidian-plugin-dev i18n 红线（所有用户可见字符串必须走 `t()`）。

### 问题
- `src/storage/annotationStore.ts` 中 `# 高亮`/`# 笔记` 分区标题、`> [返回原文]` 链接标签、`> 标签：`、`> Note:`、`## Notes`/`No notes found.` 均为硬编码中文字符串。

### 改动
- `src/i18n/index.ts` 新增 key：`storage.section.highlight`、`storage.section.note`、`storage.backToSource`、`storage.noteLabel`、`storage.tagLabel`、`storage.noNotesFound`、`storage.notesOnlyHeading`（en + zh-cn 双语）。
- `annotationStore.ts`：导出 `renderReadingNotes`/`renderAnnotationBlock`/`renderNotesOnly` 与 sidecar `serializeDocumentToMarkdown` 的分区标题、返回原文链接、标签/笔记前缀全部改为 `t(...)` 调用；`sidecarSectionTitle` 同步走 `t()`。
- `tests/markdownSidecar.test.ts`：导入 `t`，将 `# 高亮`/`# 笔记` 断言改为 `# ${t("storage.section.highlight")}`/`# ${t("storage.section.note")}`（跟随 locale，避免环境 locale 不同导致误判）。

### 验证
- `npm run build` 通过。
- `node scripts/test.mjs`：19/19 pass（修复前因 locale 非 zh-cn 导致 1 例失败，已通过 t() 断言修正）。
- manifest 合规：id=book-note、author=hellokunzai、minAppVersion=1.4.0、description 英文句号结尾；manifest/package/versions 三处版本一致（0.27.3）；`main.js` 安全扫描无禁用 API。

## 任务：插件仅对 PDF / EPUB 触发（排除 Markdown 等其它格式）

用户要求：只有电子书和 PDF 才触发插件（进度、选区批注等），其它格式（如 Markdown）不触发。

### 改动（main.ts）
- 新增 `isSupportedFile(file)` 守卫：`ext === "pdf" || SUPPORTED_BOOK_EXTENSIONS.includes(ext)`；Markdown 等其它格式返回 false。
- `SelectionToolbar` 构造增加 `isEnabled: () => this.pdfLayer.isPdfActive()`——全局浮动工具栏只在 PDF 激活时出现（EPUB 用自身 in-reader 工具栏，Markdown 不再弹出）。
- vault `modify` / `rename` / `file-open` 事件均加 `isSupportedFile` 前置判断。
- `createHighlight` / `createComment` 命令：非 PDF 激活时弹出 `notice.onlyPdfEpubAnnotations` 提示并隐藏工具栏；删除了原先的 Markdown 批注分支（已不可达）。
- 移除 Markdown 专属激活：去掉 `registerEditorExtension(createHighlightExtension)`（CM6）与 `registerMarkdownPostProcessor`（阅读视图高亮）。
- 清理死代码：`refreshActiveReadingViewHighlights`、`resolveSelection`、`activeEditor` 仅 `copySelection` 仍用、`locateRenderedSelectionInSource`、`createAnchorForSnapshot`、`findPreviewRoot`、及 markdown 选区定位辅助函数（`locateSelectionIgnoringQuoteMarkers`/`renderedOccurrenceBeforeSelection`/`selectionRoot`/`isSelectionInsideCallout`/`countOccurrences`/`nthIndexOf`）全部删除；移除未用 import（`createTextAnchor`/`refreshReadingViewHighlights`/`MarkdownPostProcessorContext`）。`unwrapStaleHighlight` 保留（handleAnnotationClick 仍用）。

### i18n / 版本 / 文档
- `src/i18n/index.ts` 新增 `notice.onlyPdfEpubAnnotations`（en + zh）。
- 版本号 `0.21.0 → 0.22.0`（manifest/package/versions.json 同步；manifest description 改 "PDF and EPUB"）。
- README.md / README.zh.md 全面改为「仅 PDF / EPUB」：删除 Markdown 批注小节、更新特性/命令表/存储示例（不再出现 `.md` 源文件批注）。

### 验证
- typecheck / build / test 全绿（12/12）。
- 安全扫描无 eval/new Function/createContextualFragment/fs/path（仅 EPUB foliate 既有 fetch）。
- `src/editor/highlightExtension.ts` 与 `readingViewHighlight.ts` 已无引用（成为死模块，保留在磁盘未删）。

### 提交（2026-08-22）
- 上一轮存储模块改造已于 2026-08-21 本地 commit（f767e8c）。
- 本轮「仅 PDF/EPUB 触发」改动已于 2026-08-22 本地提交 `ec564c7`（9 文件，+266/-1303）。
- 未推送远程、未打 tag（遵循用户死命令：未明确「提交」/「发布」指令不推送、不打 tag）。

## 任务：sidecar 文件名改为「路径段-文件名.扩展名.格式」（可读命名）

用户要求：现有编码/hash 命名（`%E6%9C%AA...md`、`00~25e8...`）不可读，改为 `.obsidian-annotations/books-未命名.pdf.json` 形式。

### 改动（src/storage/annotationStore.ts）
- 重写 `toSidecarPath(filePath)`：`normalizeVaultPath` 后按 `/` 拆分，`parts.join("-")`，后缀 `${原扩展名}.${json|md}`。
  - 例：`books/未命名.pdf` → `.obsidian-annotations/books-未命名.pdf.json`；MD 格式则是 `.md`。
  - 路径段用 `-` 连接（用户指定规则），保留原扩展名避免同名不同格式冲突。
- 删除 `toLegacySidecarPath` / `toCompactSidecarPath` / 常量 `MAX_LEGACY_SIDECAR_NAME_LENGTH` / `MAX_COMPACT_SIDECAR_PREFIX_LENGTH` / 函数 `hashPath`（全部不再引用）。
- `ensureDir` 改为递归逐段 `mkdir`（原仅建一层），扁平新命名虽无子目录但更稳健。
- `migrateExcerptFile` 移除 `encodeURIComponent(oldPath)` 替换分支（旧编码命名已不再产生）。
- `migrateAll()` 无需改：遍历 index，`oldSidecar=entry.sidecarPath` 旧路径仍可读，`newSidecar=toSidecarPath` 新路径，自动读旧写新删旧。

### 版本 / 文档
- 版本号 `0.22.0 → 0.23.0`（manifest/package/versions.json 同步追加 0.23.0 条目）。
- README.md / README.zh.md 存储结构示例改为新命名（`papers-example.pdf.json` 等），并加注释说明命名规则。

### 验证
- typecheck ✅ / build ✅ / test ✅（12/12）/ 安全扫描 ✅（无 eval/new Function/createContextualFragment）。
- 已于 2026-08-22 本地提交 `c3e1c3a`（7 文件，+53/-78）。
- 未推送远程、未打 tag（遵循死命令）。

## 任务：阅读进度字段降维（去除 pdfProgress/epubProgress 嵌套，平铺顶层）

用户要求（基于截图）：截图里 PDF 的 `pdfProgress` 与 EPUB 的 `epubProgress` 仍是整段 JSON 塞进 YAML/JSON，要求「降维」——直接把进度字段写在 YAML/JSON 顶层。
补充（第二轮）：不必加前缀，因一个文件不可能同时是 PDF 和 EPUB，每个文件只显示自己对应的属性；JSON 也做同样修改。

### 改动（src/storage/annotationStore.ts）
- `DocMeta` 接口去 `pdfProgress`/`epubProgress` 嵌套，改为扁平字段：
  - PDF：`pdfPageNumber` / `pdfTotalPages` / `pdfPercent` / `pdfLastRead`
  - EPUB：`epubCfi` / `epubChapter` / `epubPercent` / `epubLastRead` / `epubReadingTimeSeconds` / `epubEstimatedRemainingMinutes`
- `serializeDocumentToMarkdown`：PDF 有进度只写 pdf* 四个键；EPUB 有进度只写 epub* 键（无前缀、互斥）。
- `parseFrontmatter` switch 改解析扁平键；`parseMarkdownDocument` 据扁平键重组回 `PdfReadingProgress`/`EpubReadingProgress`（FileAnnotationDocument 模型不变 → 向后兼容）。
- 新增 `serializeDocumentToJson` / `parseJsonDocument`（export）：JSON 同样平铺进度字段，不再有嵌套 `pdfProgress`/`epubProgress`。
  - `parseJsonDocument` 优先读扁平键，无则回退旧嵌套 `pdfProgress`/`epubProgress`（兼容 pre-0.24 sidecar）。
- `persistDocument`（208 行）与 `migrateAll`（723 行）的 JSON 序列化由 `JSON.stringify(normalized)` 改为 `serializeDocumentToJson(normalized)`。
- `parseDocument` 的 JSON 分支由 `JSON.parse(raw)` 改为 `parseJsonDocument(raw, path)`。
- 规避 deepStrictEqual 坑：`estimatedRemainingMinutes` 为 undefined 时用展开 `...({})` 而非显式赋值 `undefined`（否则键存在但值 undefined，与缺键不等）。

### 测试
- `tests/markdownSidecar.test.ts`：断言由 `pdfProgress:`/`epubProgress:` 改为 `pdfPageNumber:`/`pdfTotalPages:`/`epubCfi:`/`epubPercent:`，并断言嵌套对象已消失。
- 新增 `tests/jsonSidecar.test.ts`：覆盖 JSON 扁平序列化、round-trip、EPUB 进度、以及**旧嵌套格式兼容**共 4 例。
- 全量测试 16/16 pass（原 12 + 新 4）。

### 版本 / 验证
- 版本号 `0.23.0 → 0.24.0`（manifest/package/versions.json 同步追加 0.24.0 条目）。
- typecheck ✅ / build ✅（main.js 重建）/ test ✅ 16/16 / 安全扫描 ✅。
- **未 git 提交**（遵循死命令：未明确「提交」不执行）。

### 补充修正（同一任务第二轮）：去掉 pdf/epub 前缀
用户基于截图反馈：「是否可以把这些前缀给去除」。

#### 决策
可以去除。因为 sidecar 文件名保留原扩展名（`*.pdf.json` / `*.epub.json`），文件类型无歧义，进度字段改为**无前缀**。

#### 无前缀字段
| 含义 | PDF sidecar | EPUB sidecar |
|---|---|---|
| 进度位置 | `pageNumber` | `cfi` |
| 总量/章节 | `totalPages` | `chapter` |
| 百分比 | `percent` | `percent` |
| 最后阅读 | `lastRead` | `lastRead` |
| 阅读时长 | — | `readingTimeSeconds` |
| 预计剩余 | — | `estimatedRemainingMinutes` |

#### 改动（src/storage/annotationStore.ts）
- `DocMeta` 接口字段去前缀：`pageNumber`/`totalPages`/`percent`/`lastRead`/`cfi`/`chapter`/`readingTimeSeconds`/`estimatedRemainingMinutes`。
- `serializeDocumentToMarkdown` / `serializeDocumentToJson` 统一写无前缀字段。
- `parseFrontmatter` switch 同时兼容**无前缀新键**与**旧带前缀键**（`pdfPageNumber`/`epubCfi` 等），无前缀优先。
- `parseMarkdownDocument` / `parseJsonDocument` 按 `cfi` 判 EPUB、`pageNumber` 判 PDF，重组为嵌套对象；同时保留对旧嵌套 `pdfProgress`/`epubProgress` 的兼容。
- `parseJsonDocument` 对 `percent`/`lastRead` 共享字段按「无前缀优先，其次 pdf*/epub*」取值。

#### 测试
- `tests/markdownSidecar.test.ts` 断言改为无前缀；新增 PDF 无前缀 round-trip、旧带前缀 YAML 兼容 2 例。
- `tests/jsonSidecar.test.ts` 断言改为无前缀；新增旧带前缀 JSON 兼容 1 例。
- 全量测试 **19/19 pass**。

#### 版本 / 验证
- 版本号 `0.24.0 → 0.25.0`（manifest/package/versions.json 同步追加 0.25.0 条目；0.24.0 仅存在于未提交工作树）。
- typecheck ✅ / build ✅（main.js 重建）/ test ✅ 19/19 / 安全扫描 ✅（无 forbidden APIs）。
- 已于 2026-08-22 本地提交 `08c2dc2`（7 文件，+622/-39，含新 tests/jsonSidecar.test.ts）。
- 旧格式读取兼容：0.23 嵌套对象、0.24 带前缀扁平、0.25 无前缀扁平三种 sidecar 均可解析。

### 补充任务（第三轮）：index.json 合并到 data.json + Sidecar 位置选项
用户需求：1. index.json 合并进插件 data.json；2. 新增选项选择 epub/pdf 批注存「同目录」还是「指定目录」。
确认细节：1. 统一一个下拉框（非 epub/pdf 分开）；2. 同目录命名接受 `.annotations.json`/`.annotations.md`；3. 开发阶段不做旧 index.json 文件迁移。

#### 改动
- `src/storage/types.ts`：新增 `SidecarLocation = "sameFolder" | "specifiedFolder"`；`AnnotationPluginSettings` 加 `sidecarLocation`（默认 `"specifiedFolder"`）；`DEFAULT_SETTINGS` 同步。
- `src/storage/annotationStore.ts`：
  - 构造签名加 `loadData: () => Promise<unknown>` 与 `saveData: (data) => Promise<void>`（泛型改非泛型）。
  - 移除 `getIndexPath` 与 `index.json` 读写；`initialize()` 从 `loadData()` 读索引（校验含 `files`）。
  - `writeIndex` 改为 `this.saveData(nextIndex)`；`ensureStoreDir` 在 sameFolder 模式跳过建目录。
  - `toSidecarPath` 双规则：sameFolder → `{src}.annotations.{ext}`；specifiedFolder → `{baseDir}/{segments}-{filename}.{ext}.{ext}`。
  - `getSidecarLocation()` / `getStorageConfigResolved()` 透出 `sidecarLocation`；`testWriteAccess` sameFolder 模式用 vault 根测试。
- `src/main.ts`：实例化注入 `() => this.loadData()` 与 `(data) => this.saveData(data)` 及 `sidecarLocation`。
- `src/settings/settingsTab.ts`：存储分区新增「批注文件位置」下拉（指定目录/与源文件同目录）；选同目录时隐藏「存储目录」设置行（`.book-note-hidden`）；`migrateIfStorageChanged` 参数补 `sidecarLocation` 并触发迁移。
- `src/i18n/index.ts`：en/zh 加 `settings.sidecarLocation.*`（name/desc/specifiedFolder/sameFolder）。
- `styles.css`：新增 `.book-note-hidden { display: none !important; }`。
- `tests/sidecarPath.test.ts`：新增 4 例（两种 location 路径生成、writeIndex 经 saveData、initialize 从 loadData 读入），全量 **23/23**。
- 版本 `0.25.0 → 0.26.0`（manifest/package/versions.json 同步）。
- README.md / README.zh.md：存储说明改为「Sidecar 位置」选项 + 两种模式示例 + 索引存 data.json 说明。

#### 验证
- typecheck ✅ / build ✅（main.js 重建）/ test ✅ 23/23 / 安全扫描 ✅。
- 已于 2026-08-22 本地提交 `2683d29`（0.26.0，13 文件，+327/-53）。
- 未推送远程、未打 tag（遵循死命令）。

## 任务：移除 JSON sidecar、仅保留 Markdown + 迁移改为仅按钮触发（0.27.0）

用户需求（确认后）：1. 只保留 Markdown 可读格式，彻底移除 JSON sidecar；2. 迁移按钮保留，仅在点击时触发（配置变更不再自动迁移）；3. 不管历史数据。
确认细节：① Markdown-only、删除 JSON；② 迁移按钮保留、只在手动点击时跑；③ 不动历史数据。

### 改动
- `src/storage/types.ts`：删除 `StorageFormat` 类型；`AnnotationPluginSettings` 去 `storageFormat`；`DEFAULT_SETTINGS` 去 `storageFormat`。
- `src/storage/annotationStore.ts`：
  - 删除 `StorageConfig.format`；`getStorageConfigResolved()` 去掉 format 分支。
  - 删除 `getFormat()`；`sidecarExtension()` 固定返回 `"md"`（并内联到 toSidecarPath）。
  - `persistDocument` 固定 `serializeDocumentToMarkdown`；删除 `serializeDocumentToJson` / `parseJsonDocument` 整段（约 120 行）；`parseDocument` 去掉格式判断只走 Markdown；`migrateAll` 去掉 oldFormat 维度，仅当 `oldSidecar !== newSidecar` 时移动。
- `src/main.ts`：构造配置去掉 `format: this.settings.storageFormat`。
- `src/settings/settingsTab.ts`：删除「批注存储格式」下拉框及其 `storageFormat` 引用；`sidecarLocation`/`storagePath` 变更不再调 `migrateIfStorageChanged`（已删除该方法），仅保留手动「迁移已有批注」按钮。
- `src/i18n/index.ts`：删除 en/zh 全部 `settings.storageFormat.*` 键。
- `tests/jsonSidecar.test.ts`：删除（JSON 已移除）；`tests/sidecarPath.test.ts`：去掉 `format` 配置键、扩展名断言改 `.md`。
- 版本 `0.26.0 → 0.27.0`（manifest/package/versions.json 同步追加 0.27.0）。
- README.md / README.zh.md：存储说明改为「仅 Markdown 可读格式」+ 手动迁移按钮提示 + 示例全改 `.md`。

### 验证
- typecheck ✅ / build ✅（main.js 重建）/ test ✅ 18/18 / 安全扫描 ✅（无 eval/new Function/createContextualFragment）。
- **未 git 提交**（遵循死命令：未明确「提交」不执行）。

## 任务：迁移按钮移到「批注文件位置」右侧（UI 调整）

用户需求：把「迁移已有批注」按钮从独立设置行移动到「批注文件位置」下拉框右侧。

### 改动
- `src/settings/settingsTab.ts`：
  - 「批注文件位置」Setting 在 `.addDropdown(...)` 后追加 `.addButton(...)`，按钮文案 `settings.storage.migrate`，点击调用 `migrateAndNotify()`。
  - 删除原来独立的「迁移已有批注」Setting 行（含 name/desc）。
- `src/i18n/index.ts`：
  - 更新 `settings.storage.migrate.desc`（en + zh），改为手动触发说明：
    - en: "Move existing annotation sidecars to the current folder. Click the button on the right to run."
    - zh: "将已有批注 sidecar 移动到当前目录。点击右侧按钮执行。"

### 验证
- typecheck ✅ / build ✅（main.js 重建）/ test ✅ 18/18 / 安全扫描 ✅。
- **未 git 提交**。

## 任务：迁移按钮改名 + 默认存储目录改为 .booknote（0.27.0 内小调整）

用户需求：1.「迁移已有批注」按钮改名为「迁移数据」；2. 默认存储目录由 `.obsidian-annotations` 改为 `.booknote`。

### 改动
- `src/i18n/index.ts`：
  - `settings.storage.migrate`：en `"Migrate data"` / zh `"迁移数据"`。
  - `settings.storagePath.desc` / `settings.storagePath.placeholder`、错误提示 `notice.storageNotWritable` 中的 `.obsidian-annotations` 全部改为 `.booknote`（en + zh）。
- `src/storage/types.ts`：`DEFAULT_SETTINGS.storagePath` 由 `".obsidian-annotations"` 改为 `".booknote"`。
- `src/storage/annotationStore.ts`：`DEFAULT_STORE_DIR` 改为 `".booknote"`；文件头注释与 `toSidecarPath` 示例同步更新。
- `tests/sidecarPath.test.ts`：测试用默认目录与断言路径全部改为 `.booknote`。
- 文档同步：
  - `README.md` / `README.zh.md`：默认目录示例改为 `.booknote/`。
  - `SUBMISSION_CHECKLIST.md`：默认目录改为 `.booknote/`。
  - `src/AGENTS.md` / `src/storage/AGENTS.md`：描述改为 `.booknote` 持久化后端/入口。
  - `docs/linkedin-carousel/generate-linkedin-carousel.mjs` 与 `exports/slide-6.svg`：目录文本改为 `.booknote/`。

### 验证
- typecheck ✅ / build ✅（main.js 重建）/ test ✅ 18/18 / 安全扫描 ✅。
- **未 git 提交**。

## 任务：同目录 sidecar 去掉 `.annotations` 中间后缀

用户需求：数据文件不用加 `.annotations` 后缀（截图显示 `蛊真人-蛊真人.epub.annotations.md`）。

### 决策
同目录模式下 sidecar 直接命名为 `{源文件}.md`（如 `蛊真人-蛊真人.epub.md`），不再插入 `.annotations`。`.epub` 与 `.epub.md` 是不同文件，不会覆盖源文件。

### 改动
- `src/storage/annotationStore.ts`：`toSidecarPath` 的 `sameFolder` 分支由 `${normalized}.annotations.${sidecarExt}` 改为 `${normalized}.${sidecarExt}`；注释示例同步更新。
- `tests/sidecarPath.test.ts`：sameFolder 断言改为 `books/未命名.pdf.md`；initialize 测试用例 sidecarPath 也改新命名。
- `README.md` / `README.zh.md`：同目录示例与说明改为 `<source>.md` / `<源文件>.md`。

### 验证
- typecheck ✅ / build ✅（main.js 重建）/ test ✅ 18/18 / 安全扫描 ✅。
- **未 git 提交**。

## 任务：默认存储目录去掉点前缀，改为 `booknote`

用户需求：默认存储目录由 `.booknote` 改为 `booknote`（截图显示输入框内为 `.booknote`）。

### 改动
- `src/storage/types.ts`：`DEFAULT_SETTINGS.storagePath` 由 `".booknote"` 改为 `"booknote"`。
- `src/storage/annotationStore.ts`：`DEFAULT_STORE_DIR` 改为 `"booknote"`；文件头注释与 `toSidecarPath` 示例同步更新。
- `src/i18n/index.ts`：存储目录描述、placeholder、写入失败提示中的 `.booknote` 全部改为 `booknote`（en + zh）。
- `tests/sidecarPath.test.ts`：默认目录参数与断言路径全部改为 `booknote`。
- 文档同步：
  - `README.md` / `README.zh.md`：默认目录示例与说明改为 `<vault>/booknote/`。
  - `SUBMISSION_CHECKLIST.md`：默认目录改为 `booknote/`。
  - `src/AGENTS.md` / `src/storage/AGENTS.md`：描述改为 `booknote` 持久化后端/入口。
  - `docs/linkedin-carousel/generate-linkedin-carousel.mjs` 与 `exports/slide-6.svg`：目录文本改为 `booknote/`。

### 验证
- typecheck ✅ / build ✅（main.js 重建）/ test ✅ 18/18 / 安全扫描 ✅。
- **未 git 提交**。

## 任务：修复发布失败，升级到 0.27.1 重新推送

问题经过：
1. 用户要求推送并打 tag；首次 `v0.27.0` 推送后因仓库是 fork、Actions 未启用而未触发 workflow。
2. 启用 Actions 后重新推 `v0.27.0`，Release workflow 运行但失败（#2）。
3. 诊断：0.27.0 核心代码改动（删除 JSON、booknote 目录、去 `.annotations` 后缀等）实际未提交到 git，HEAD 仍是 0.26.0 代码；CI build 后 `main.js` 与提交中的 `main.js` 不一致，`git diff --exit-code -- main.js` 失败。
4. 修复本地 `.git` 对象数据库损坏：commit `70cda26` 引用的 `docs` 子树对象 `112f76c...` 在本地丢失，导致无法 `git add`/`read-tree`；通过 fresh clone 从远程复制 pack 文件恢复。

### 最终发布操作（0.27.1）
- 版本号统一升到 `0.27.1`（manifest / package / package-lock / versions.json）。
- 将工作树中 0.27.0 全部未提交改动正式 commit：
  - 提交 `5ba0e71`「feat(storage): release 0.27.1 — Markdown-only sidecars, manual migrate, booknote dir」（17 文件，+78/-546）。
- 推送 `main`：`70cda26..5ba0e71 main -> main`。
- 删除远程/本地 `v0.27.0`，新建并推送 annotated tag `v0.27.1`。
- 本地 `npm run verify` 通过：`book-note release metadata verified: 0.27.1`。

### 当前状态
- 远程 `main = 5ba0e71`，`refs/tags/v0.27.1 -> 5ba0e71`。
- 等待 GitHub Actions「Release Obsidian Plugin」workflow 为 tag `v0.27.1` 运行结果。

## 任务：版本升到 0.27.2 重新触发 CI（续上）

- 本地 `main` 当时领先 origin 11 个提交（含 0.27.1 提交 `5ba0e71`），这些此前未推送。
- 版本号统一升 `0.27.2`（manifest / package / package-lock / versions.json），其中 package-lock 版本同步是 0.27.0 失败的历史坑点，已一并修。
- 提交 `b9f7b48`「chore: bump version to 0.27.2」。
- 推送 `main`：`5ba0e71..b9f7b48 main -> main`（落后 11 个提交全部补齐）。
- 删本地 `v0.27.1`、建并推送 `v0.27.2`（指向 `b9f7b48`）。
- 远程核验：`refs/heads/main` 与 `refs/tags/v0.27.2` 均指向 `b9f7b48f...`，一致。
- `npm run verify` 通过：18/18 测试、typecheck 干净、`release metadata verified: 0.27.2`。
- 环境无 `gh` CLI，无法登录态查 CI 状态；release.yml 触发条件为 push tag `*`，关键校验 `git diff --exit-code -- main.js` 因代码已提交不会再失败。
- 待用户到 GitHub Actions 页面确认 `v0.27.2` 的 Release run 是否成功（红/绿）。

{
	"name": "yh-inklight",
	"version": "0.20.0",
	"private": true,
	"description": "Non-invasive Obsidian annotations for Markdown, PDF, and EPUB with a unified sidebar, semantic tags, deep links, and sidecar storage.",
	"main": "main.js",
	"scripts": {
		"build": "node esbuild.config.mjs production",
		"dev": "node esbuild.config.mjs",
		"test": "node scripts/test.mjs",
		"typecheck": "tsc --noEmit",
		"verify": "npm test && npm run typecheck && node scripts/verify-release.mjs"
	},
	"devDependencies": {
		"@codemirror/state": "6.5.0",
		"@codemirror/view": "6.38.6",
		"@types/node": "^20.14.10",
		"builtin-modules": "^4.0.0",
		"esbuild": "^0.25.0",
		"obsidian": "^1.7.2",
		"tslib": "^2.8.1",
		"typescript": "^5.6.3"
	},
	"dependencies": {
		"foliate-js": "^1.0.1"
	}
}
